# This is a Databricks asset bundle definition for streamlit_demo.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: streamlit_demo

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-1131652458261111.11.azuredatabricks.net

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: https://adb-1131652458261111.11.azuredatabricks.net
